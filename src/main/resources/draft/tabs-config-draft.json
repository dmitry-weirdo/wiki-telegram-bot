{
  "commandTabs": [ // вкладки команд - перегружают друг друга в порядке массива. Более приоритетны, чем вкладки общих данных (см. следующий элемент).
    { // Самый высокий приоритет - команды конкретного бота - перегружают последующую вкладку итд по порядку
      "name": "Дюся болталка",
      "type": "Command" // формат данных на вкладке
    },
    { // Самый низкий приоритет
      "name": "Общая болталка",
      "type": "Command"
    },
  ],

  "commonTabs": [ // вкладки общих данных - если не найдена команда, результаты выдаются из всех них в одном ответе
    // порядок в массиве определяет порядок выдачи в результате
    // !!! Перегрузки нет, выдадутся все результаты из всех этих вкладок
    { // 1 в списке
      "name": "Страницы вики",
      "type": "WikiPages" // формат данных на вкладке
    },
    { // 2 в списке
      // Если не хотим выдавать видео в ответах - убираем этот элемент из массива!
      "name": "UAHelp YouTube",
      "type": "Command" // формат как в командах!
    },
    { // 3 в списке
      "name": "Список чатов по городам Германии",
      "type": "Chats" // формат для чатов одинаковый
    },
    { // 4 в списке
      "name": "Список чатов по странам",
      "type": "Chats" // формат для чатов одинаковый
    }
  ]
}
